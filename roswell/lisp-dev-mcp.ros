#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#

(progn
  (ros:ensure-asdf)
  #+quicklisp
  (ql:quickload (list "40ants-lisp-dev-mcp/main")
                :silent t))

(in-package #:cl-user)

(import '40ants-lisp-dev-mcp/main::main)

;;; vim: set ft=lisp lisp:
